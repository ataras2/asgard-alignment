from setuptools import setup, find_packages

setup(
    name="asgard_alignment",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # List your dependencies heres
    ],
)
